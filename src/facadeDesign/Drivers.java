package facadeDesign;

// Drivers Enum might not be used. Original idea was to store drivers' names
// and pickup distances, but distances would presumably change
// and enum values ought to be constant.

public enum Drivers {

}
